import styled from 'styled-components';

export const Container = styled.div`
  min-height: 100vh;
  padding: 32px 16px;
  max-width: 1200px;
  margin: 0 auto;
  @media (max-width: 600px) {
    padding: 8px 2px;
  }
`; 